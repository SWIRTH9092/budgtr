<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include("./partials/head.ejs", {title: "Budget Index List"}) %>
    </head>
<body>
    <div class="container">
        <h1>Budgtr</h1>
        <a href="/budgets/new"><button type="button">New</button></a>
        <table>
            <% allBudgetItems.forEach((v, index) =>{ %> 
                <tr>
                    <td class="first-child"><%=v.date%></td>
                    <td class="first-child">
                        <a href="/budgets/<%=index%>"><%=v.name%></a>
                    </td>
                    <td class="last-child"><%=v.amount%></td>
                    <td>
                        <form action="/budgets/<%= index %>?_method=DELETE" method="POST">
                            <input type="submit" value="Delete">
                    </td>
                </tr>
            <% }) %> 
        </table>
    </div>
</body>
</html>